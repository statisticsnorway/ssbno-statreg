apiVersion: data.nais.io/v1
kind: Postgres
metadata:
  name: statreg-api-test-db
  namespace: ssbno
spec:
  cluster:
    audit:
      enabled: true
    majorVersion: "17"
    resources:
      cpu: 200m
      diskSize: 2Gi
      memory: 2Gi
  database:
    collation: nb_NO
